

<h1> 
    Tipos de Objetivo Eliminados:

    <a href="/goback"><img class="icon" src="/icons/back.png"></a>
</h1>

<table>
    <tr>
        <th>
            Borrado
        </th>
        <th>
            Nombre
        </th>
        <th>
            
        </th>
    </tr> 

    <% for (var i in targettypes) { %> 
        <% var targettype = targettypes[i]; %>      

        <tr>
            <td>
                <%= moment(targettype.deletedAt).format("DD-MM-YYYY") %>
            </td>
            <td>
                <%= targettype.name %>
            </td>
            <td>
                <% if (session.user && session.user.isAdmin) { %>
                <a href="/trash/targettypes/<%= targettype.id %>?_method=POST"><img class="icon" src="/icons/restore.png"></a>
                <% } %>


                <% if (session.user && session.user.isAdmin) { %>
                <a href="/trash/targettypes/<%= targettype.id %>?_method=DELETE"
                   onClick="return confirm('¿Seguro que quiere borrar DEFINITIVAMENTE el tipo de objetivo: <%= targettype.name %>?');"><img class="icon" src="/icons/kill.png"></a>
                <% } %>
            </td>
         </tr> 
    <% } %>

</table>

